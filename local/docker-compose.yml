version: '3'
services:
  postgres:
    image: postgres:16.8
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: johndoe
      POSTGRES_PASSWORD: mypassword
      POSTGRES_DB: resourcedb
